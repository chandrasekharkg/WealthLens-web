family.toml
__pycache__/
*.egg-info/
.venv
.venv/
node_modules/
frontend/dist/
frontend/e2e/.tmp/

# Local-only credentials (e.g. the WLC read PAT used to seed the WLC_REPO_TOKEN Actions secret). Never tracked.
.localsecrets/

# Playwright run output (a stale .last-run.json was tracked from the phase-4 commit; CI keeps failures as an artifact instead)
frontend/test-results/
frontend/playwright-report/
