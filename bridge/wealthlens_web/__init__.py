"""WealthLens-web bridge — the read-only API over WLC's lens (see openspec/specs/bridge-api).

Skeleton stage: the specs and ADRs define this package's contract; implementation follows them and seeds
from the reviewed report-server prototype (WLC PR #1) with ADR-0004's hardening.
"""
